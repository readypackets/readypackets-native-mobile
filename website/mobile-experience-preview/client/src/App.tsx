/**
 * Secure Editorial Console — permanent self-hostable ReadyPackets demo shell.
 * The experience uses representative local data only and never connects to Portal accounts.
 */
import Home from "./pages/Home";

export default function App() {
  return <Home />;
}
